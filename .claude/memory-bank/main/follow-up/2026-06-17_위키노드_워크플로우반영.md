# RIPER 플랜 — 컴파일 노드(2026-06-17) → claude-personal-integrated-workflow 반영

> 생성: 2026-06-17 · 트리거: `/compile-wiki` 노드 변동(신규 5 + 수정 9)
> **반영 대상 레포(실측)**: `D:\Fork\claude-personal-integrated-workflow` (remote `git.anxi.kr/Anxi/Harness.git`, branch `main`) — **개발 중인 최신 워크플로**. WIKI 레포의 `.claude`(미러)가 아님.
> 본 문서 위치: WIKI `FOLLOWUP_DIR`(계획 산출물). **구현은 위 워크플로 레포에서**, RULE-4(소스 수정 전 승인) 게이트 이후.
> 상태: PLAN(승인 대기) — `.claude/` 미변경.

> ⚠️ **정정 이력**: 1차 초안은 WIKI의 `.claude`(미러)를 대상으로 잡아 이미 존재하는 자산(turn-budget 등)을 신규 제안하는 오류가 있었음. 사용자 지적으로 실제 워크플로 레포를 인벤토리해 전면 재작성.

---

## RESEARCH — 실측 인벤토리 (워크플로 레포)

**hooks (15)** — settings.json 연결 확인:
- SessionStart: `session-start.sh`
- PreToolUse: `Bash→sonar-guard.sh` · `PowerShell→sonar-guard-powershell.sh` · `Read|Glob|Grep→codegraph-gate.sh` · `Write|Edit→write-approval-reminder.sh` · `Task→subagent-dispatch-gate.sh` · `(all)→turn-budget-gate.sh`
- PostToolUse: `(all)→tool-history-recorder.sh` · `Write|Edit→dict-sync-check.sh, gate-check.sh` · `Read→dict-blk-announce.sh`
- **Stop: `output-arm-gate.sh` + `turn-budget-gate.sh`**

**rules (7)**: engine · gameplay · narrative · prototype · shader · `quality-sentinel.md` · `reporter.md`
**commands**: `riper/`(research·innovate·plan·execute·review·strict·workflow) · `memory/`(list·recall·save) · `rebase.md`
**skills (11)**: add-lang · agent-eval · `cache-aligner` · doc-context · handover · `output-arm` · sync-global-codegraph · try · `turn-budget` · visualize-graph
**agents (18)**: 3-Tier 스튜디오 전원(producer·quality-sentinel·reporter·writer·lead/gameplay/graphics/engine-programmer·*-director·unity-* 등)

---

## 재조정 — 이번 컴파일 원자 vs 이미 구현된 자산

| 컴파일 노트(원자) | 워크플로 레포 현황 | 액션 |
|---|---|---|
| [[최소컨텍스트_진입규약]] (crg A2) | ✅ **`turn-budget-gate.sh`**(헤더 "CRG2강제") + `turn-budget` 스킬. 턴=결정론 프록시, off/`N`/`N:block` 모드, PreToolUse+Stop | **없음**(구현 완료). WIKI 노트가 이 hook의 지식 짝 |
| HR A5 CacheAligner | ✅ **`cache-aligner` 스킬** | 없음(정렬 확인만) |
| superpowers A3 서브에이전트 격리 | ✅ **`subagent-dispatch-gate.sh`**(Task matcher) | 없음(정렬 확인만) |
| 출력 압축(출력_표현_압축) | ✅ **`output-arm` 스킬 + `output-arm-gate.sh`** | 없음 |
| Sonar 개명 | ✅ **`sonar-guard.sh`** / `sonar-guard-powershell.sh` | 없음(WIKI 노트와 일치 확인됨) |
| HR A2 SmartCrusher · HR A3 결정론 불변식 | 🟡 `output-arm`·`gate-check.sh`와 인접(압축/검증) | 정렬 확인(별도) |
| **[[증거_기반_완료_검증]] (superpowers A1)** | ❌ `riper/review.md`에 Quality Gate 체크리스트는 있으나 *증거 첨부 강제*·무근거 완료 표현 금지 없음 | **갭 → P1** |
| **[[루트_원인_우선_디버깅]] (superpowers A2)** | ❌ 디버깅 방법론 자산 부재(commands·skills·rules·hooks 어디에도 없음) | **갭 → P2** |
| [[SKILL_md_작성원칙]] (agent-skills A2~6) | 🟡 `agent-eval` 스킬(corpus.json)이 "평가→은퇴"(Atom 4)와 인접 | **레퍼런스 → P3(저)** |
| [[사고_외재화_패턴]] | 설계 레퍼런스 | 없음 |

**결론**: 컴파일 원자 다수가 이 레포에 **이미 hook/skill로 구현**돼 있다(WIKI는 지식, 레포는 구현 — 정상적 짝). 신규 작업은 **진짜 갭 2개(P1·P2)** 로 한정.

---

## INNOVATE — 재사용 우선 적대 검증 (갭만)

| 갭 | 최소-diff 흡수안 | 반증(신규 자산?) | 결론 |
|---|---|---|---|
| 증거 기반 완료 검증 | `commands/riper/review.md` Quality Gate에 +몇 줄(실행 명령·출력 첨부 의무, "should work/아마" 무근거 완료 표현 금지) | Stop hook(`evidence-gate.sh`)로 하드 강제도 가능하나 Stop엔 이미 2개. soft(review.md)부터, 하드는 오탐 데이터 본 뒤 | **review.md +최소줄**(필요 시 reporter.md 연계). 신규 hook 보류 |
| 루트 원인 디버깅 | `commands/riper/research.md`에 "디버깅 분기" 1절(4단계 + 3회 실패→아키텍처 에스컬레이션, [[작업_규모별_워크플로]] 규모 게이팅) | 방법론이 커지면 `skills/debug` 신설 여지 | **research.md +1절**(reuse). 신규 스킬은 보류(증분 시 승격) |
| SKILL 작성 원칙 | `agent-eval` 또는 신규 skill 작성 시 [[SKILL_md_작성원칙]] 참조 | 상시 강제 불요 | **레퍼런스만**(자산 신설 없음) |

원칙: **신규 자산 0개** 목표 — 두 갭 모두 기존 `riper/*` 커맨드 +최소줄로 흡수. vault_mandate 재사용·토큰 통제 불훼손.

---

## PLAN — 실행 항목 (워크플로 레포 기준 · 경로·LOC·토큰 Δ)

> 모든 경로는 `D:\Fork\claude-personal-integrated-workflow\` 기준.

### P1. 완료 증거 의무 — `.claude/commands/riper/review.md` [경량]
- Quality Gate 체크리스트에 항목 추가: "각 ✅는 *실행한 명령 + 그 출력*을 근거로 첨부. 미실행 추정('should work'·'아마 통과')으로 완료 선언 금지."
- Review Report Format에 `### 검증 증거` 칸 1개 추가(실행 명령·핵심 출력 라인).
- (선택) `rules/reporter.md`에 "증거 없는 완료 Entry 기록 금지" 1줄.
- LOC: +6~8줄 | 턴당 토큰 Δ: **+0%**(커맨드 온디맨드) | 난이도 낮음 · 위험 낮음 | 충돌 無([[증거_기반_완료_검증]]·6대원칙 §3).

### P2. 루트 원인 디버깅 분기 — `.claude/commands/riper/research.md` [경량]
- "## 디버깅 모드 분기" 1절: 조사→패턴→가설→구현 4단계, "3회 수정 실패 → 국소버그 아님, 아키텍처로 에스컬레이션", 소형은 [[작업_규모별_워크플로]] 기준 직접 수정(4단계 면제).
- LOC: +12~15줄 | 턴당 토큰 Δ: **+0%** | 난이도 낮음 · 위험 낮음 | 충돌 無.

### P3. SKILL 작성 원칙 레퍼런스 [선택·저]
- `skills/agent-eval` 또는 신규 스킬 작성 시 [[SKILL_md_작성원칙]](설명=트리거·점진적 로딩·은퇴·부정 케이스)을 참조 링크로. 자산 신설 없음.

### 보류 (근거)
- `evidence-gate.sh` 신규 Stop hook: Stop에 이미 2개(output-arm·turn-budget). soft(P1) 운용 → 오탐/누락 데이터 누적 후 하드 강제 재평가.
- `skills/debug` 신규 스킬: research.md +1절로 충분. 방법론 증분 시 승격.

---

## EXECUTE — 체크리스트 (⚠️ 별도 레포 · RULE-4 승인 후)
- [ ] **작업 레포 전환**: `cd D:\Fork\claude-personal-integrated-workflow` (WIKI 아님). 브랜치 확인(main) → 필요 시 작업 브랜치.
- [ ] P1: `riper/review.md` Quality Gate + Report Format +증거 칸 → (선택) reporter.md 1줄
- [ ] P2: `riper/research.md` 디버깅 분기 1절
- [ ] P3: (선택) agent-eval ↔ SKILL_md_작성원칙 참조
- [ ] 각 변경은 [[증거_기반_완료_검증]] 본인 원칙대로 — 실행/렌더 확인 출력 첨부

## REVIEW — 검수 기준
- review.md가 무근거 완료를 차단하는가(증거 칸 비면 APPROVED 불가).
- research.md 디버깅 분기가 소형 버그에 4단계를 강제하지 않는가([[작업_규모별_워크플로]] 정합).
- **신규 자산 0개** 유지(기존 riper 커맨드 +최소줄) — 재사용 우선 준수.
- 턴당 베이스 토큰 무증가(커맨드 온디맨드) — vault_mandate 토큰 통제 불훼손.
- 이미 구현된 자산(turn-budget·cache-aligner·subagent-dispatch·output-arm·sonar)과 **중복 신설하지 않았는가**.

---

## 연계
- 지식 출처: [[증거_기반_완료_검증]]·[[루트_원인_우선_디버깅]]·[[SKILL_md_작성원칙]]·[[최소컨텍스트_진입규약]]·[[사고_외재화_패턴]]
- 상위 철학: [[LLM_통제_철학_6대원칙]](§1 토큰·§3 Hooks>Prompts) · [[이중방어_Defense_in_Depth]] · [[구체지시_우선_원칙]](반복 주입) · [[작업_규모별_워크플로]]
- 상태머신: [[RIPER_5단계_상태머신]]
